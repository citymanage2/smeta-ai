/**
 * Отступы области прокрутки (`<main>` в Layout).
 *
 * Отдельным модулем, а не в `Layout.tsx`: за самим Layout тянутся глобальные
 * поллеры и уведомления со звуком (AudioContext), а цифры нужны и тем видам,
 * которым нужен только отступ.
 */

/**
 * Верхний отступ области прокрутки (`<main>`).
 *
 * `position: sticky` отсчитывает смещение от content edge окна прокрутки, то
 * есть уже ЗА паддингом, а `overflow` обрезает контент по padding edge. Без
 * вычитания над прилипшей шапкой остаётся полоса в 32px, сквозь которую видно
 * уезжающие строки. Проверено в Chrome: gapFromMainPaddingEdge = 32.
 */
export const MAIN_PADDING_TOP = 32;

/** Боковой отступ окна прокрутки. Виды, которым нужны поля поуже (сетка смет),
 *  считают свой отрицательный margin от него, а не от повторённой цифры. */
export const MAIN_PADDING_X = 24;
