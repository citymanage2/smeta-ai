# Smeta AI - Веб-сервис для автоматизированного формирования смет

Веб-сервис для автоматизированного формирования перечня работ и материалов, смет, и сравнительного анализа проектной документации с использованием Claude API.

## 📋 Функциональность

- ✅ **Аутентификация**: Единый пароль для пользователей и отдельный для администраторов
- ✅ **Загрузка документов**: Поддержка PDF, Excel, XML (ГрандСмета), GSN форматов
- ✅ **Формирование Перечня**: Автоматическое создание структурированного перечня работ и материалов
- ✅ **Расчет Сметы**: Поиск цен в прайсах и интернете, расчет НДС
- ✅ **Сравнительный анализ**: Сравнение проекта со сметой, выявление расхождений
- ✅ **История запросов**: Сохранение всех выполненных задач
- ✅ **Админ-панель**: Просмотр статистики, экспорт данных в CSV
- ✅ **Адаптивный интерфейс**: Работает на мобильных устройствах и десктопе

## 🚀 Быстрый старт

### Локальное развертывание

#### Требования
- Python 3.9+
- PostgreSQL 12+
- CLAUDE_API_KEY от Anthropic

#### Установка зависимостей

```bash
cd backend
pip install -r requirements.txt
```

#### Настройка переменных окружения

```bash
# Копируем пример конфигурации
cp .env.example .env

# Редактируем .env с вашими значениями
nano .env
```

Необходимые переменные окружения:
```
DATABASE_URL=postgresql://user:password@localhost/smeta_ai
CLAUDE_API_KEY=sk-XXXXXXXXXXXX
USER_PASSWORD=ваш_пароль_для_пользователей
ADMIN_PASSWORD=ваш_пароль_для_администраторов
JWT_SECRET=ваш_секретный_ключ_для_jwt
CLAUDE_MODEL=claude-opus-4-5
```

#### Инициализация базы данных

```bash
# Создание базы данных PostgreSQL
createdb smeta_ai
```

#### Запуск сервера

```bash
cd backend
python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Сайт будет доступен по адресу `http://localhost:8000`

## 📦 Развертывание на Render.com

### Шаг 1: Подготовка репозитория GitHub

1. Создайте репозиторий на GitHub
2. Загрузите все файлы проекта

```bash
git push origin main
```

### Шаг 2: Развертывание на Render

1. Перейдите на [Render.com](https://render.com)
2. Создайте новый Web Service
3. Выберите GitHub репозиторий smeta-ai
4. Выберите branch `main`

### Шаг 3: Конфигурация

В настройках сервиса на Render.com:

**Переменные окружения:**
- `CLAUDE_API_KEY` - ваш API ключ Anthropic
- `USER_PASSWORD` - пароль для обычных пользователей
- `ADMIN_PASSWORD` - пароль для администраторов
- `JWT_SECRET` - генерируемый секрет для JWT (используйте `python -c "import secrets; print(secrets.token_hex(32))"`)

**Database:**
- Создается автоматически как PostgreSQL в render.yaml

После настройки Render автоматически разберется с:
- Установкой зависимостей
- Инициализацией базы данных
- Запуском сервера

## 📁 Структура проекта

```
smeta-ai/
├── backend/
│   ├── main.py                    # FastAPI приложение
│   ├── auth.py                    # JWT аутентификация
│   ├── database.py                # Конфигурация БД
│   ├── models.py                  # ORM модели SQLAlchemy
│   ├── routes/
│   │   ├── auth.py                # Маршруты входа
│   │   ├── tasks.py               # Основные задачи
│   │   └── admin.py               # Админ-панель
│   ├── services/
│   │   ├── claude_service.py      # Взаимодействие с Claude API
│   │   ├── file_parser.py         # Парсинг файлов
│   │   ├── excel_builder.py       # Генерация Excel
│   │   └── pdf_builder.py         # Генерация PDF
│   ├── pricelists/                # Прайс-листы
│   │   ├── price_works.xlsx       # Цены на работы
│   │   └── price_materials.xlsx   # Цены на материалы
│   └── requirements.txt           # Зависимости
├── frontend/
│   ├── index.html                 # Интерфейс
│   ├── app.js                     # Логика фронтенда
│   └── styles.css                 # Стили
├── render.yaml                    # Конфигурация Render
└── README.md                      # Этот файл
```

## 🔐 Аутентификация

### Вход для пользователей
- Пароль: значение переменной `USER_PASSWORD`
- Получаемый токен действует 24 часа

### Вход для администраторов
- Пароль: значение переменной `ADMIN_PASSWORD`
- Получает доступ к админ-панели

### JWT токены
- Алгоритм: HS256
- Срок действия: 24 часа
- При неверном пароле - сообщение об ошибке без указания какой пароль неверен

## 📊 Этапы обработки документов

### Этап 1: Формирование Перечня работ и материалов
- Claude анализирует загруженные документы
- Извлекаются все работы и материалы
- Результат сохраняется в Excel с тремя листами:
  - Полный перечень
  - Перечень работ (отфильтрованный)
  - Перечень материалов (отфильтрованный)

### Этап 2: Расчет Сметы
- На основе Перечня и прайс-листов
- Поиск цен в provided прайсах и интернете
- Учет НДС (22% для большинства работ)
- Выделение позиций без цены красным цветом

### Этап 3: Сравнительный анализ
- Сравнение проекта и сметы/перечня
- Выявление отсутствующих позиций
- Выявление лишних позиций
- Расхождения в объёмах
- Несоответствия единиц измерения
- Критические замечания и рекомендации

## 🛠️ API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/logout` - Выход из системы

### Задачи
- `POST /api/tasks/process` - Обработка документов
- `GET /api/tasks/download/{file_id}` - Скачивание файла
- `GET /api/tasks/history` - История запросов

### Админ-панель (требует admin flag)
- `GET /api/admin/requests` - Список всех запросов
- `GET /api/admin/request/{request_id}` - Детали запроса
- `GET /api/admin/export-csv` - Экспорт в CSV
- `GET /api/admin/stats` - Статистика

## 💾 База данных

**Таблица requests:**
- id - уникальный идентификатор
- created_at - дата и время создания
- input_type - тип входных данных
- uploaded_files - JSON массив загруженных файлов
- requested_outputs - JSON массив выбранных результатов
- status - статус обработки (pending, success, error)
- claude_prompt - первые 5000 символов промпта
- claude_response - первые 5000 символов ответа
- output_files - JSON с информацией о результирующих файлах
- error_message - сообщение об ошибке
- user_comment - комментарий пользователя

**Таблица output_files:**
- id - уникальный идентификатор
- request_id - ссылка на запрос
- file_name - имя файла (содержит дату)
- file_path - путь к файлу на сервере
- file_type - тип файла (excel_list, excel_estimate, pdf_comparison)
- created_at - дата создания

## ⚙️ Конфигурация Claude API

- **Модель**: claude-opus-4-5 (или claude-sonnet-4-5)
- **Temperature**: 0.2 (для детерминированности)
- **max_tokens**:
  - Перечень: 8000
  - Смета: 8000
  - Сравнительный анализ: 4000
- **Timeout**: 120 секунд

## 🔧 Прайс-листы

### Структура price_works.xlsx
| Наименование | Ед. изм. | Подрядчик 1 | Подрядчик 2 | ... | Самая низкая цена |

### Структура price_materials.xlsx
| Наименование | Ед. изм. | Цена |

Прайс-листы хранятся в `backend/pricelists/` и читаются при каждом запросе.

## 📝 Форматы входных данных

Поддерживаемые форматы:
- **PDF** (.pdf) - документы, спецификации
- **Excel** (.xlsx, .xls) - спецификации, готовые перечни
- **XML** (.xml) - ГрандСмета
- **GSN** (.gsn) - ГрандСмета (упакованный zip архив)

## 🎨 Интерфейс

### Главный экран (для пользователей)
- Загрузка до 10 файлов одновременно (drag-and-drop)
- Выбор типа входных данных (radio buttons)
- Выбор результатов (checkboxes)
- Опциональный комментарий
- Индикатор прогресса с 4 шагами
- История последних 10 запросов
- Скачивание готовых файлов

### Админ-панель
- Статистика: всего запросов, успешно, ошибок, % успеха
- Таблица всех запросов с фильтрацией по дате
- Просмотр деталей каждого запроса
- Экспорт истории в CSV

## 🐛 Обработка ошибок

- Невалидный JSON от Claude - повтор запроса (до 2 попыток)
- Неподдерживаемый формат файла - сообщение об ошибке
- Timeout при запросе к Claude - сообщение об ошибке
- Все ошибки логируются в БД

## 🌍 Локализация

Весь интерфейс и сообщения на русском языке.

## 📋 Лицензия

MIT License

## 👥 Автор

Создано как решение техническому заданию для Smeta AI.

## 📞 Поддержка

При возникновении проблем:
1. Проверьте переменные окружения
2. Убедитесь в наличии подключения к БД
3. Проверьте логи в консоли
4. Убедитесь что CLAUDE_API_KEY действителен

---

**Версия**: 1.0.0  
**Дата**: 2025
