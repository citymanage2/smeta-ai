# План: Единый редактор таблиц

**Спека:** `specs/2026-08-02-edinyy-redaktor-tablic.md`
**Исследование:** `thoughts/research/2026-08-02-edinyy-redaktor-tablic.md`
**Статус:** planning
**Размер:** L — полный цикл bulletproof (стадии 1–12)
**Ветка:** `feature/unified-table-editor`

---

## Challenge Log

**Проблема.** Пять типов документов редактируются пятью разными таблицами через три разные точки входа, а смета вдобавок хранится в двух местах и молча расходится. Итог: непредсказуемые цифры в смете и тройная стоимость любой доработки.

**Выбранное решение.** Трёхслойный единый редактор: оболочка `DocumentEditor` (общее поведение) → адаптер документа (колонки, правила, доступные действия) → `react-data-grid` как слой отрисовки + собственный слой буфера обмена. Плюс единый источник правды по смете и одна страница на смету.

**Рассмотренные альтернативы:**

1. **Fortune-Sheet («Excel в браузере»)** — отклонено: работает с файлом, а не с данными; умная логика сметы (связь работа→материалы, пересчёт объёмов материалов, ABC, отрицательный объём, проверки) на свободных ячейках не воспроизводится. Библиотека уже в проекте и остаётся тестовой вкладкой в админке.
2. **Единый плоский формат данных для всех пяти типов** — отклонено: уничтожает умные функции сметы, ради которых продукт и существует. Пользователь выбрал вариант «одна оболочка, два адаптера».
3. **Платный грид с готовым буфером обмена (AG Grid Enterprise ~$1000/год, MUI X Premium ~$588/год)** — отклонено: $600–1000 в год за одну недостающую функцию во внутреннем инструменте на несколько человек. Свой слой буфера — ~150 строк с тестами.
4. **TanStack Table (headless)** — отклонено: пришлось бы заново писать виртуализацию, редактирование ячеек и навигацию, которые в react-data-grid уже есть и работают в проде проекта.
5. **Оставить `GenericGrid` как ядро** — отклонено: самописная таблица не умеет ни навигации по ячейкам, ни перетаскивания, ни связей между строками; дотянуть её дороже, чем удалить.

**Почему выбранное лучше.** Переиспользуем то, что уже работает (react-data-grid покрывает ~90% списка умений), удаляем 653 строки самописной таблицы и две дублирующие модалки, платим ноль за лицензии, и получаем архитектуру, в которую «разный функционал у разных типов» ложится штатно, а не костылём.

**Проверка на «код ради кода».** Каждая фаза привязана к критериям приёмки спеки. Правки, не привязанные к критериям (косметика, попутный рефакторинг соседних модулей), в этот план не входят — отдельной задачей.

---

## Проблемы, которые план обязан закрыть

| # | Проблема | Решение | Фаза | Статус |
|---|---|---|---|---|
| 1 | Смета хранится в двух местах и расходится | Единый источник правды — `EstimateVersion`; миграция с dry-run отчётом | Ф5 | pending |
| 2 | Пять разных таблиц | Одна оболочка + адаптеры; `GenericGrid` удаляется | Ф2, Ф4–Ф7 | pending |
| 3 | Три точки входа, одна из них через iframe | Один компонент, две оболочки (встроенный + на весь экран) | Ф2, Ф3 | pending |
| 4 | Три экрана на одну смету | Страница карточки поглощает страницу задачи | Ф3 | pending |
| 5 | Undo/redo и расчёт итогов написаны трижды | Один модуль истории, один модуль расчёта | Ф1, Ф2, Ф8 | pending |
| 6 | Режим просмотра обходится правкой адреса | Проверка прав на сервере | Ф1 | **done** |
| 7 | Тихая перезапись чужих правок | `rev` документа + баннер присутствия + 409 при расхождении | Ф1, Ф2 | бэкенд done, UI в Ф2 |
| 8 | Оригинал заказчика перезаписывается | Входные файлы — только чтение | Ф1 | **done** |
| 9 | Мусорная первая строка в перечнях из Гранд-сметы | Определение границы многострочной шапки | Ф4 | pending |
| 10 | react-data-grid не умеет вставку диапазона из Excel | Собственный слой буфера обмена | Ф2 | pending |
| 11 | Нет выгрузки-ведомости, коэффициента, аналогов, работы с прайсом | Четыре новые функции | Ф8–Ф11 | pending |
| 12 | Проценты 3% захардкожены в трёх местах | Настройка на уровне проекта | Ф8 | pending |

---

## Ключевые архитектурные решения

### Модель «документ»

```ts
type DocumentKind = 'list' | 'completeness' | 'estimate' | 'optimization' | 'summary-section'

interface DocumentRef {
  cardId: string          // карточка сметы
  kind: DocumentKind      // тип документа = набор колонок и действий
  versionId?: string      // конкретная версия (по умолчанию — активная)
}
```

Единый API поверх существующих хранилищ — новый роутер `backend/app/routers/documents.py`:

| Метод | Назначение |
|---|---|
| `GET /documents/{card_id}/{kind}` | метаданные: версии, права, коэффициент, `rev`, статус задачи, кто редактирует |
| `GET /documents/{card_id}/{kind}/rows` | строки активной или указанной версии |
| `PUT /documents/{card_id}/{kind}/draft` | автосохранение черновика (без пересборки файла) |
| `POST /documents/{card_id}/{kind}/apply` | «Применить»: черновик → рабочие строки, пересборка xlsx, запись истории, `rev++` |
| `GET /documents/{card_id}/{kind}/history` | история правок |
| `POST /documents/{card_id}/{kind}/revert/{entry_id}` | откат |
| `POST /documents/{card_id}/{kind}/heartbeat` | присутствие «кто редактирует» |
| `POST /documents/{card_id}/{kind}/export` | выгрузка-ведомость |

### Схема БД (миграция 042)

| Таблица | Изменение | Зачем |
|---|---|---|
| `estimate_versions` | `draft_rows JSON NULL`, `draft_updated_at`, `draft_user_id` | черновик до «Применить» |
| `estimate_versions` | `rev INTEGER NOT NULL DEFAULT 0` | защита от перезаписи чужих правок |
| `estimate_versions` | `coefficient JSON NULL` — `{work, material, scope}` | обратимый коэффициент |
| `task_history` | `user_id INTEGER NULL`, `user_name VARCHAR(120) NULL`, `row_ref VARCHAR(80) NULL`, `field VARCHAR(60) NULL` | «кто, когда, что именно» |
| `projects` | `overhead_pct NUMERIC(5,2) DEFAULT 3`, `transport_pct NUMERIC(5,2) DEFAULT 3` | проценты на уровне проекта |
| `document_locks` (новая) | `card_id, kind, user_id, user_name, heartbeat_at` | баннер присутствия |

Все `IF NOT EXISTS`, шаблон — `backend/alembic/versions/013_add_composite_index_tasks_project_estimation.py`.

### Матрица возможностей по типам документов

| Возможность | Перечень | Полнота | Смета | Оптимизация | Разделы сводной |
|---|:--:|:--:|:--:|:--:|:--:|
| Поиск, вкладки Р/М, галочки | ✅ | ✅ | ✅ | ✅ | ✅ |
| Undo/redo, черновик, история | ✅ | ✅ | ✅ | ✅ | ✅ |
| Копипаст диапазона, Tab/стрелки | ✅ | ✅ | ✅ | ✅ | ✅ |
| Добавить / удалить строку, перетаскивание | ✅ | ✅ | ✅ | ✅ | ✅ |
| Автопересчёт «цена × объём», итоги | — | — | ✅ | ✅ | ✅ |
| Удалить работу с материалами, ABC, отрицательный объём | — | — | ✅ | ✅ | ✅ |
| Вкладки версий | — | — | ✅ | ✅ | — |
| Коэффициент | — | — | ✅ | ✅ | ✅ |
| Выгрузка-ведомость | ✅ | ✅ | ✅ | ✅ | ✅ |
| Прайс: добавить из / добавить в | — | — | ✅ | ✅ | ✅ |
| Найти аналоги, «↺ Цена», «Исправить пустые цены» | — | — | ✅ | ✅ | — |

---

## Фазы

### Фаза 1. Фундамент бэкенда: документ, черновик, история, права, блокировка [x]

- **Статус:** completed (2026-08-02) · 23 теста, полный прогон 724 passed / 9 skipped
- **Файлы:**
  `backend/alembic/versions/042_unified_document_editor.py` (новый),
  `backend/app/models/estimate_version.py`, `backend/app/models/history.py`, `backend/app/models/project.py`,
  `backend/app/models/document_lock.py` (новый),
  `backend/app/routers/documents.py` (новый), `backend/app/main.py`,
  `backend/app/schemas/document.py` (новый),
  `backend/app/services/document_service.py` (новый),
  `backend/app/utils/permissions.py`,
  `backend/tests/test_documents_api.py` (новый)
- **Изменения:**
  - миграция 042 по таблице выше;
  - `document_service` — разрешение `DocumentRef` → конкретное хранилище (generic-версия / estimate-версия / раздел сводной), чтение, черновик, применение, история, откат;
  - серверная проверка прав на запись: менеджер — только свои (`can_access` по `owner_id`), `head_of_sales` и `admin` — любые; режим просмотра больше не зависит от параметра в адресе;
  - блокировка записи, пока задача в статусе `processing` / `pending`;
  - `rev`: `apply` с устаревшим `rev` → HTTP 409 с именем того, кто сохранил;
  - присутствие: heartbeat раз в 20 секунд, запись «протухает» за 90 секунд;
  - **входные файлы (`file_slot='input'`) переводятся в режим только чтения** — ветка перезаписи `TaskInputFile` в `estimate_versions.py:212-229` удаляется;
  - проценты проекта: поля + подстановка по умолчанию 3%.
- **TDD (пишем первыми):**
  `test_draft_then_apply_creates_history_entry`, `test_apply_with_stale_rev_returns_409`,
  `test_manager_cannot_write_foreign_document`, `test_head_of_sales_can_write_foreign_document`,
  `test_write_blocked_while_task_processing`, `test_input_file_never_overwritten`,
  `test_revert_restores_previous_rows`, `test_lock_expires_after_90s`,
  `test_project_percentages_default_to_3`
- **Гейты:** `pytest` ✅ · `ruff check .` ✅ · `python -m py_compile app/main.py` ✅
- **Impact:** трогает модели, используемые `estimate_versions.py`, `workflow_cards.py`, `summary.py`. Старые эндпоинты продолжают работать без изменений — новый роутер добавляется рядом. Проверить, что миграция накатывается на копию прод-базы.
- **Промпт запуска:**
  ```
  Читай plans/2026-08-02-edinyy-redaktor-tablic.md, Фаза 1, и specs/2026-08-02-edinyy-redaktor-tablic.md.
  Реализуй Фазу 1. Сначала тесты (список в разделе TDD), потом код.
  Не трогай файлы вне списка в разделе «Файлы».
  Старые эндпоинты должны продолжать работать без изменений.
  После завершения: 1) самопроверка по критериям приёмки спеки, 2) проверка, что найденные баги реальны,
  3) анализ влияния на estimate_versions.py / workflow_cards.py / summary.py, 4) все гейты Tier 1.
  ```
- **Отклонения от плана (осознанные):**
  1. **История правок хранится записями «одно применение = одна запись»** с перечнем
     изменений внутри, а не отдельными колонками `row_ref` / `field`. Причина: точка
     отката — это применение целиком; строки на каждую ячейку размножили бы снимки.
     Детализация «строка 12, Цена работ: 3000 → 2500» при этом сохранена.
  2. **Добавлена колонка `task_history.document_kind`** (в плане её не было): у одной
     задачи может быть несколько документов, их истории нельзя смешивать.
  3. **Введены ограничители глубины истории** `HISTORY_DEPTH=20` и `SNAPSHOT_DEPTH=10`.
     Снимок «как было» весит столько же, сколько документ; без ограничителя смета на
     2000 строк раздувала бы базу на десятки мегабайт. Откат доступен для 10 последних
     правок, ещё 10 остаются справкой без отката.
  4. **Старый эндпоинт `PUT .../rows` теперь двигает `rev`.** На переходный период он
     пишет в те же строки; без этого открытый рядом новый редактор молча затёр бы его
     правки.
  5. **Защита от гонки сделана условным `UPDATE ... WHERE rev = client_rev`,** а не только
     сравнением в коде: между проверкой и коммитом было окно, в котором два «Применить»
     проходили оба.
  6. **`summary-section` отложен до Фазы 7** — в списке файлов Фазы 1 моделей сводной нет,
     и её хранилище (секции внутри одной записи) требует своих колонок черновика.
     Сервис спроектирован под добавление этого типа без переделки.

---

### Фаза 2. Ядро редактора + адаптеры + буфер обмена + перечень [ ]

- **Статус:** pending
- **Файлы:**
  `frontend/src/components/editor/DocumentEditor.tsx` (новый),
  `frontend/src/components/editor/EditorToolbar.tsx` (новый),
  `frontend/src/components/editor/EditorHistoryPanel.tsx` (новый),
  `frontend/src/components/editor/PresenceBanner.tsx` (новый),
  `frontend/src/components/editor/clipboard.ts` (новый),
  `frontend/src/components/editor/adapters/genericAdapter.ts` (новый),
  `frontend/src/components/editor/adapters/estimateAdapter.ts` (новый),
  `frontend/src/components/editor/adapters/types.ts` (новый),
  `frontend/src/stores/documentEditor.ts` (новый),
  `frontend/src/api/documents.ts` (новый),
  `frontend/src/components/kanban/CardStageContent.tsx` (точки входа),
  `frontend/src/components/card/GenericEditorModal.tsx` (**удаляется**),
  `frontend/src/components/estimate/GenericGrid.tsx` + `.css` + `.test.tsx` (**удаляются**),
  тесты: `frontend/src/__tests__/editorClipboard.test.ts`, `editorDraft.test.tsx`, `editorPerf2000.test.tsx`
- **Изменения:**
  - оболочка: тулбар (поиск, вкладки Р/М, счётчик строк, undo/redo, «Применить», «Скачать», «на весь экран»), панель действий над выделением, панель истории, баннер присутствия, надпись «идёт расчёт»;
  - `react-data-grid` как слой отрисовки, интерфейс адаптера: `columns`, `rowKey`, `canEdit`, `onCellChange`, `computeTotals`, `actions`;
  - `genericAdapter` — плоские строки `{row_id, cells}`, колонки из фактических данных;
  - слой буфера: перехват `copy`/`paste` на контейнере, якорь из `onSelectedCellChange`, разбор и сборка TSV, ограничение вставки нередактируемых колонок;
  - черновик: автосохранение через 800 мс после паузы, «Применить» с обработкой 409, предупреждение `beforeunload` и при закрытии полноэкранного режима;
  - **перечень** (`LIST_FROM_GRAND`, `LIST_FROM_PROJECT`) переводится на новый редактор; старый путь остаётся за флагом `VITE_LEGACY_EDITOR=1`.
- **TDD:** вставка TSV меньше/больше выделенной области; вставка нечисловых значений в числовую колонку; вставка в нередактируемую колонку игнорируется; copy диапазона даёт корректный TSV; черновик сохраняется один раз на серию правок; 409 показывает понятное сообщение; рендер 2000 строк укладывается в бюджет; отмена/повтор восстанавливают состояние.
- **Гейты:** `npx tsc --noEmit` ✅ · `npm run lint` ✅ · `npm test` ✅
- **Impact:** `CardStageContent` — единственная точка входа в редактор из канбана; убедиться, что канбан-карточка и страница карточки продолжают открывать перечень. `EstimateOptimizer.tsx` пока не трогаем — там остаётся ветка сметы.
- **Промпт запуска:**
  ```
  Читай plans/2026-08-02-edinyy-redaktor-tablic.md, Фаза 2, и спеку.
  Реализуй ядро редактора, адаптер generic, слой буфера обмена и перевод перечня.
  Сначала тесты. Не трогай файлы вне списка.
  GenericGrid и GenericEditorModal удаляются только после того, как перечень работает на новом редакторе.
  Старый редактор должен оставаться доступен под флагом VITE_LEGACY_EDITOR.
  После завершения: самопроверка по спеке, проверка реальности багов, анализ влияния, все гейты Tier 1.
  ```

---

### Фаза 3. Одна страница на смету [ ]

- **Статус:** pending
- **Файлы:**
  `frontend/src/pages/ProjectCardPage.tsx`, `frontend/src/pages/TaskStatus.tsx` (сокращается до редиректа),
  `frontend/src/components/card/StageProcessingPanel.tsx` (новый — ход обработки, стоимость, ошибки, перезапуск),
  `frontend/src/App.tsx` (роуты), `frontend/src/components/kanban/CardStageContent.tsx`,
  тесты: `frontend/src/__tests__/CardPageMerge.test.tsx`
- **Изменения:**
  - на странице карточки под блоком этапа появляются: ход обработки (сворачиваемый, раскрыт при ошибке), стоимость и время, кнопки «Перезапустить» / «Возобновить», встроенный редактор документа этапа;
  - чат-уточнение удаляется из интерфейса;
  - `/tasks/:taskId/status` → редирект на `/projects/:projectId/cards/:cardId?stage=...`, состояние в адресе (`stage`, `version`, `tab`) — для критерия «ссылка открывает то же состояние»;
  - канбан-доска и скачивание из неё не меняются.
- **TDD:** старый адрес редиректит на нужный этап; при ошибке задачи блок хода обработки раскрыт; ссылка со `stage`/`version`/`tab` восстанавливает состояние; чата в разметке нет.
- **Гейты:** `tsc` ✅ · `lint` ✅ · `npm test` ✅
- **Impact:** ссылки на страницу задачи есть в уведомлениях (`utils/notify.ts`), в `UnassignedInbox`, `ProjectDetail`, `SmetaList`, `CardStageContent` — обойти все `navigate('/tasks/...')` и проверить. Роут не удаляем, только редиректим.
- **Промпт запуска:**
  ```
  Читай plans/2026-08-02-edinyy-redaktor-tablic.md, Фаза 3, и спеку.
  Объедини страницу задачи со страницей карточки сметы. Сначала тесты.
  Роут /tasks/:taskId/status не удалять — он должен редиректить с сохранением этапа.
  Найди ВСЕ переходы на /tasks/.../status в проекте и проверь каждый.
  После завершения: самопроверка, проверка багов, анализ влияния, гейты Tier 1.
  ```

---

### Фаза 4. Полнота + починка многострочных шапок Гранд-сметы [ ]

- **Статус:** pending
- **Файлы:**
  `backend/app/utils/xlsx_generic.py`, `backend/tests/test_xlsx_generic_headers.py` (новый),
  `frontend/src/components/editor/adapters/genericAdapter.ts`,
  `frontend/src/components/kanban/CardStageContent.tsx`
- **Изменения:**
  - определение границы шапки: шапкой считается блок строк до первой строки с содержательными данными; строка, состоящая только из последовательных номеров колонок (`1,2,3…`), в данные не попадает; многострочные заголовки склеиваются в одно название колонки;
  - `CHECK_LIST_COMPLETENESS` и `CHECK_PROJECT_COMPLETENESS` переводятся на новый редактор.
- **TDD:** файл с двухстрочной шапкой + строкой нумерации → первая строка данных настоящая; файл с обычной однострочной шапкой не ломается (регресс на существующих перечнях); пустые заголовки получают осмысленные имена; повторный разбор уже сохранённого файла идемпотентен.
- **Гейты:** `pytest` ✅ · `ruff` ✅ · `tsc` ✅ · `npm test` ✅
- **Impact:** парсер используется в `init-from-result` и `init-from-input`. Проверить на реальных файлах из `.business` / прод-выгрузки: перечни, созданные до фикса, при повторном открытии не должны «съезжать».
- **Промпт запуска:**
  ```
  Читай plans/2026-08-02-edinyy-redaktor-tablic.md, Фаза 4, и спеку.
  Почини разбор многострочных шапок Гранд-сметы и переведи документ «Полнота» на новый редактор.
  Сначала тесты, включая регресс на однострочных шапках.
  Проверь на реальных xlsx: перечни, разобранные старым парсером, не должны сломаться.
  После завершения: самопроверка, проверка багов, анализ влияния, гейты Tier 1.
  ```

---

### Фаза 5. Смета: единый источник правды + миграция данных [ ]

**Самая рискованная фаза. Единственное необратимое действие в плане.**

- **Статус:** pending
- **Файлы:**
  `backend/app/services/document_service.py`, `backend/app/routers/tasks.py` (`PATCH /estimate-items` → тонкая обёртка над документом),
  `backend/app/services/task_processor.py` (по завершении расчёта сразу создаётся рабочая версия),
  `backend/scripts/migrate_estimate_items.py` (новый — dry-run и применение),
  `frontend/src/pages/TaskStatus.tsx` (inline-таблица **удаляется**),
  `frontend/src/components/editor/adapters/estimateAdapter.ts`,
  `frontend/src/components/editor/actions/PriceActions.tsx` (новый — «↺ Цена», «Исправить пустые цены»),
  `frontend/src/components/card/EstimateEditorModal.tsx` (**удаляется**),
  тесты: `backend/tests/test_estimate_single_source.py`, `backend/tests/test_migrate_estimate_items.py`
- **Изменения:**
  - `ESTIMATE_FROM_LIST` по завершении расчёта сразу создаёт `EstimateVersion(file_slot='estimate')`;
  - `progress_data['items']` остаётся как «что выдал ИИ», источником правды быть перестаёт;
  - `PATCH /estimate-items` не пишет напрямую, а вызывает документный сервис (обратная совместимость для внешних вызовов);
  - смета переводится на новый редактор, inline-таблица и модалка на iframe удаляются;
  - «↺ Цена» и «Исправить пустые цены (N)» переезжают в панель действий редактора;
  - **скрипт миграции:** сначала `--dry-run` — отчёт по всем сметам, где `progress_data['items']` и версия расходятся (номер сметы, число расхождений, суммы); затем применение с выбором победителя по времени изменения и полным бэкапом обоих состояний в `task_history`.
- **TDD:** правка через любой путь меняет одно хранилище; `task.cost`, содержимое редактора и скачанный файл дают одно число; dry-run не меняет данные; миграция идемпотентна; для сметы без расхождений миграция проходит без потерь; конфликтная смета попадает в отчёт и не мигрируется автоматически.
- **Гейты:** `pytest` ✅ · `ruff` ✅ · `tsc` ✅ · `lint` ✅ · `npm test` ✅
- **Impact:** затрагивает `task.cost` → дашборд, канбан, воронка, сводная себестоимость (берёт `rows` версий). Прогнать все тесты проекта. **Перед применением миграции — бэкап базы и согласование списка активных тендерных смет с пользователем.**
- **Промпт запуска:**
  ```
  Читай plans/2026-08-02-edinyy-redaktor-tablic.md, Фаза 5, и спеку.
  Сделай EstimateVersion единственным источником правды по смете и переведи смету на новый редактор.
  Сначала тесты. Скрипт миграции обязан иметь режим --dry-run и быть идемпотентным.
  НЕ запускай миграцию на реальных данных — только подготовь скрипт и отчёт dry-run.
  Проверь влияние на task.cost: дашборд, канбан, воронка, сводная себестоимость.
  После завершения: самопроверка, проверка багов, анализ влияния, ВСЕ тесты проекта, гейты Tier 1.
  ```

---

### Фаза 6. Оптимизация: версии и предложения в едином редакторе [ ]

- **Статус:** pending
- **Файлы:**
  `frontend/src/pages/EstimateOptimizer.tsx` (**сокращается**: логика уходит в страницу карточки и ядро),
  `frontend/src/components/estimate/VersionTabs.tsx`, `OptimizationToolbar.tsx`, `OptimizationProposalsPanel.tsx`, `EstimateComparison.tsx`,
  `frontend/src/components/editor/adapters/estimateAdapter.ts`,
  `frontend/src/stores/estimateEditor.ts` (**удаляется**, заменён на `documentEditor`)
- **Изменения:** вкладки версий, панель предложений, сравнение версий и тулбар шагов подключаются к ядру и **видны в любом способе открытия**, включая встроенный вид и полноэкранный (сейчас скрыты при `embed`).
- **TDD:** вкладки версий видны во встроенном и полноэкранном режиме; переключение версии не теряет непринятый черновик (предупреждение); применение предложений создаёт версию и переключает на неё; сравнение версий открывается из редактора.
- **Гейты:** `tsc` ✅ · `lint` ✅ · `npm test` ✅
- **Impact:** `EstimateOptimizer` перестаёт быть страницей — проверить, что все ссылки на `/tasks/:id/estimate` ведут на страницу карточки.
- **Промпт запуска:**
  ```
  Читай plans/2026-08-02-edinyy-redaktor-tablic.md, Фаза 6, и спеку.
  Подключи оптимизацию к единому редактору: вкладки версий, панель предложений, сравнение.
  Вкладки версий должны быть видны ВСЕГДА, в любом способе открытия.
  Сначала тесты. После завершения: самопроверка, проверка багов, анализ влияния, гейты Tier 1.
  ```

---

### Фаза 7. Сводная: разделы на едином редакторе [ ]

- **Статус:** pending
- **Файлы:** `frontend/src/components/summary/SummaryEditorTabs.tsx`, `frontend/src/stores/summaryEditorStore.ts`, `backend/app/routers/summary.py`
- **Изменения:** вкладки разделов переходят на `DocumentEditor` (kind `summary-section`); бланк «Сводная» (`SummarySheet.tsx`) **не трогаем**; общий undo/redo и черновик распространяются на разделы.
- **TDD:** правка раздела попадает в итог сводной; переключение вкладок не теряет черновик; бланк «Сводная» считает так же, как до изменений (регресс по цифрам).
- **Гейты:** `tsc` ✅ · `lint` ✅ · `npm test` ✅ · `pytest` ✅
- **Impact:** `calcSummary` — регресс-тест на неизменность итогов обязателен.
- **Промпт запуска:**
  ```
  Читай plans/2026-08-02-edinyy-redaktor-tablic.md, Фаза 7, и спеку.
  Переведи вкладки разделов сводной на единый редактор. Бланк «Сводная» НЕ трогай.
  Обязателен регресс-тест: итоги сводной до и после изменений совпадают до копейки.
  После завершения: самопроверка, проверка багов, анализ влияния, гейты Tier 1.
  ```

---

### Фаза 8. Коэффициент + проценты на уровне проекта [ ]

- **Статус:** pending
- **Файлы:**
  `frontend/src/components/editor/CoefficientControl.tsx` (новый),
  `frontend/src/utils/estimateCalc.ts` (новый — единый расчёт итогов),
  `frontend/src/components/estimate/EstimateSummary.tsx`, `AdditionalExpenses.tsx`,
  `frontend/src/pages/ProjectDetail.tsx` (блок «Настройки проекта»),
  `backend/app/routers/projects.py`, `backend/app/services/document_service.py`, `backend/app/utils/xlsx_exporter.py`,
  тесты: `frontend/src/__tests__/coefficient.test.ts`, `backend/tests/test_project_percentages.py`
- **Изменения:**
  - коэффициент как обратимая настройка документа: общий, отдельно на работы и материалы, область применения — все строки или отмеченные галочками; хранится в `estimate_versions.coefficient`; исходные цены не изменяются;
  - коэффициент участвует в итогах, в выгрузке и в скачиваемом xlsx;
  - проценты накладных и транспортных — в настройках проекта, по умолчанию 3%, подставляются в документы проекта;
  - расчёт итогов выносится в один модуль; три захардкоженных места удаляются.
- **TDD:** коэффициент 1,01 меняет цены и итог, снятие возвращает исходные; коэффициент к выделенным трогает только их; коэффициент попадает в xlsx и в выгрузку; изменение процентов проекта пересчитывает итоги документов; у существующих проектов проценты = 3.
- **Гейты:** `tsc` ✅ · `lint` ✅ · `npm test` ✅ · `pytest` ✅ · `ruff` ✅
- **Impact:** единый модуль расчёта затрагивает смету, оптимизацию и сводную — регресс по цифрам на всех трёх.
- **Промпт запуска:**
  ```
  Читай plans/2026-08-02-edinyy-redaktor-tablic.md, Фаза 8, и спеку.
  Сделай коэффициент (обратимая настройка документа) и проценты на уровне проекта.
  Вынеси расчёт итогов в один модуль и удали три захардкоженных места (GenericGrid уже удалён,
  остаются TaskStatus и EstimateSummary).
  Обязателен регресс: итоги смет без коэффициента не изменились.
  После завершения: самопроверка, проверка багов, анализ влияния, гейты Tier 1.
  ```

---

### Фаза 9. Выгрузка-ведомость на всех документах [ ]

- **Статус:** pending
- **Файлы:**
  `frontend/src/components/editor/ExportBuilderModal.tsx` (новый — обобщение `CustomExportModal`),
  `frontend/src/components/summary/CustomExportModal.tsx` (**удаляется** после переноса),
  `backend/app/routers/documents.py`, `backend/app/utils/xlsx_exporter.py`,
  тесты: `frontend/src/__tests__/exportBuilder.test.tsx`, `backend/tests/test_document_export.py`
- **Изменения:**
  - окно в два шага для любого документа: тип строк (работы / материалы / всё), выбор столбцов из фактических колонок документа, выбор позиций **двумя способами** (галочками в таблице до открытия и галочками в предпросмотре), настраиваемая шапка (по умолчанию всё включено: объект, проект, дата, итоговая строка), предпросмотр, скачивание;
  - пресеты «Ведомость работ» и «Ведомость материалов» в один клик;
  - фильтр «Разделы» — только в сводной (в отдельном документе не показывается);
  - цены выгружаются с учётом коэффициента; оформление — корпоративное.
- **TDD:** пресет «Ведомость работ» даёт только работы; выбор столбцов отражается в файле; отключение элементов шапки убирает их из файла; выбор позиций галочками в таблице переносится в окно; коэффициент учтён в выгруженных ценах; для перечня подставляются его собственные колонки.
- **Гейты:** `tsc` ✅ · `lint` ✅ · `npm test` ✅ · `pytest` ✅ · `ruff` ✅
- **Impact:** сводная теряет свою кнопку выгрузки в пользу общей — проверить, что старый сценарий выгрузки сводной даёт тот же файл.
- **Промпт запуска:**
  ```
  Читай plans/2026-08-02-edinyy-redaktor-tablic.md, Фаза 9, и спеку.
  Обобщи существующий CustomExportModal сводной на все пять типов документов.
  Сначала тесты. Проверь, что выгрузка сводной после переноса даёт тот же результат, что раньше.
  После завершения: самопроверка, проверка багов, анализ влияния, гейты Tier 1.
  ```

---

### Фаза 10. Работа с прайсом: добавить в прайс и добавить из прайса [ ]

- **Статус:** pending
- **Файлы:**
  `frontend/src/components/editor/actions/AddToPriceList.tsx` (новый),
  `frontend/src/components/editor/actions/AddFromPriceList.tsx` (новый),
  `frontend/src/api/catalog.ts`,
  `backend/app/routers/prices_catalog.py` (bulk-эндпоинт), `backend/app/utils/unit_normalizer.py`,
  тесты: `backend/tests/test_price_bulk_add.py`, `frontend/src/__tests__/priceActions.test.tsx`
- **Изменения:**
  - **в прайс:** выделенные позиции уходят пакетом; работы — в псевдо-подрядчика `Из смет`, материалы — ценой; единицы измерения нормализуются существующим `unit_normalizer`; при совпадении наименования цена **добавляется** ещё одной, а не затирает; результат — сводка «добавлено N, обновлено M, пропущено K»; право — у любого менеджера, действие пишется в историю;
  - **из прайса:** поиск по прайсу (умный поиск по смыслу через `match-preview`), выбор нескольких позиций, вставка **после текущей строки** с наименованием, ед. изм. и ценой.
- **TDD:** повторное добавление той же работы даёт вторую цену у «Из смет», а не перезапись; «м2» и «м²» нормализуются в одну единицу; пакет из 50 позиций проходит одним запросом; вставка из прайса встаёт после текущей строки; выбор нескольких позиций вставляет их в выбранном порядке.
- **Гейты:** `pytest` ✅ · `ruff` ✅ · `tsc` ✅ · `lint` ✅ · `npm test` ✅
- **Impact:** прайс общий на всех и участвует в расчёте смет — засорение прайса повлияет на все будущие сметы. Псевдо-подрядчик «Из смет» изолирует эти цены от настоящих прайсов подрядчиков; проверить, что `min_price` считается корректно и что поиск цен в расчёте сметы не начал предпочитать «Из смет».
- **Промпт запуска:**
  ```
  Читай plans/2026-08-02-edinyy-redaktor-tablic.md, Фаза 10, и спеку.
  Сделай «Добавить в прайс» (пакетно, псевдо-подрядчик «Из смет», нормализация ед.изм.,
  дубль = ещё одна цена) и «Добавить из прайса» (поиск по смыслу, вставка после текущей строки).
  Сначала тесты. Обязательно проверь влияние на подбор цен при расчёте сметы.
  После завершения: самопроверка, проверка багов, анализ влияния, гейты Tier 1.
  ```

---

### Фаза 11. Найти аналоги через ИИ [ ]

- **Статус:** pending
- **Файлы:**
  `backend/app/services/analogs_service.py` (новый),
  `backend/app/routers/documents.py`, `backend/app/services/job_queue.py`, `backend/app/worker.py`,
  `frontend/src/components/editor/actions/FindAnalogs.tsx` (новый),
  `frontend/src/components/editor/AnalogsPanel.tsx` (новый),
  тесты: `backend/tests/test_analogs_service.py`, `frontend/src/__tests__/analogsPanel.test.tsx`
- **Изменения:**
  - действие «Найти аналоги» над выделенными позициями в смете и оптимизации (в перечне и полноте отсутствует);
  - цель поиска — **дешевле при том же результате** (замена материала или технологии), источник — интернет через Claude с веб-поиском;
  - до 3 вариантов на позицию: наименование, ед. изм., цена, разница в деньгах, обоснование, источник;
  - кнопка **«Заменить»** подставляет вариант вместо исходной строки (через черновик, значит откатывается);
  - выполняется через существующую очередь как фоновая задача с прогрессом; перед запуском — подтверждение с оценкой («будет обработано N позиций, это займёт ~M минут»); расход учитывается в существующем лимите веб-поиска.
- **TDD:** действие недоступно для перечня и полноты; запрос уходит по всем выделенным; ответ без вариантов обрабатывается без ошибки; «Заменить» правит черновик и откатывается через Ctrl+Z; при исчерпанном лимите веб-поиска действие блокируется с понятным сообщением; повторный запуск на тех же строках не дублирует записи.
- **Гейты:** `pytest` ✅ · `ruff` ✅ · `tsc` ✅ · `lint` ✅ · `npm test` ✅
- **Impact:** веб-поиск — основная статья расходов (смета ~$10, 68% — веб-поиск). Без ограничителя массовый запуск на 500 позиций даст ощутимый счёт. Проверить учёт в `api_call_log` и в лимитах.
- **Промпт запуска:**
  ```
  Читай plans/2026-08-02-edinyy-redaktor-tablic.md, Фаза 11, и спеку.
  Сделай «Найти аналоги»: фоновая задача через job_queue, до 3 вариантов на позицию,
  кнопка «Заменить» через черновик. Обязательны: подтверждение с оценкой объёма,
  учёт расхода в существующем лимите веб-поиска, недоступность для перечня и полноты.
  Сначала тесты. После завершения: самопроверка, проверка багов, анализ влияния, гейты Tier 1.
  ```

---

### Фаза 12. Чистка, снятие флага, финальные проверки [ ]

- **Статус:** pending
- **Файлы:** удаление остатков `EstimateOptimizer.tsx`, `stores/estimateEditor.ts`, флага `VITE_LEGACY_EDITOR`; обновление `CLAUDE.md`; `plans/archive/`
- **Изменения:** снятие переходного флага после подтверждения пользователем по каждому типу; удаление мёртвого кода; обновление навигации в `CLAUDE.md`; стадии 8–12 bulletproof.
- **Гейты:** все Tier 1 ✅ · `npx madge --circular src/` ✅ · `npm audit --audit-level=high` ✅ · `pip-audit` ✅ · `/security-review` ✅
- **Impact:** после снятия флага откат возможен только через git — снимать только после подтверждения на всех пяти типах.
- **Промпт запуска:**
  ```
  Читай plans/2026-08-02-edinyy-redaktor-tablic.md, Фаза 12, и спеку.
  Сними переходный флаг, удали мёртвый код, обнови CLAUDE.md.
  Прогони Integration Check (стадия 8): каждый критерий приёмки спеки — где именно закрыт в коде.
  Затем Code Review (стадия 9) и Security Scan (стадия 10) в свежем контексте.
  Деплой НЕ выполнять — только по отдельной команде пользователя.
  ```

---

## Порядок и параллельность

```
Ф1 (бэкенд-фундамент)
 └─> Ф2 (ядро + перечень)
      ├─> Ф3 (одна страница)          ← можно параллельно с Ф4
      ├─> Ф4 (полнота + шапки)
      └─> Ф5 (смета, единый источник) ← только после Ф3
           └─> Ф6 (оптимизация)
                └─> Ф7 (сводная)
                     ├─> Ф8 (коэффициент + проценты)
                     ├─> Ф9 (выгрузка)      ← после Ф8 (цены с коэффициентом)
                     ├─> Ф10 (прайс)        ← можно параллельно с Ф9
                     └─> Ф11 (аналоги)      ← после Ф10
                          └─> Ф12 (чистка)
```

Ф3 и Ф4 независимы — можно вести в двух сессиях. Ф9 и Ф10 независимы между собой.

---

## Что проверить руками перед сдачей каждого типа документа

1. Открыть документ из канбан-карточки, со страницы карточки и по прямой ссылке — везде один и тот же редактор.
2. Внести правку, не применять, закрыть — предупреждение появилось, правка не потерялась.
3. Применить, открыть историю — запись с автором, временем, строкой и значениями «было → стало»; откат работает.
4. Скачать файл — цифры совпадают с тем, что на экране.
5. Открыть тот же документ во втором браузере под другим пользователем — баннер присутствия, сохранение поверх отклонено.
6. Открыть документ на 2000 строк — прокрутка и ввод без задержек.

---

## Changelog

| Дата | Фаза | Изменения |
|---|---|---|
| 2026-08-02 | — | План создан (стадии 1–3 bulletproof: исследование, спека, план) |
| 2026-08-02 | Ф1 | Миграция 042; модели `DocumentLock`, черновик/`rev`/коэффициент у версии, автор правки в истории, проценты проекта; `document_service` + роутер `/documents`; входной файл заказчика больше не перезаписывается ни новым, ни старым путём. 23 новых теста, полный прогон 724 passed / 9 skipped, ruff чист |

---

## Итог

**Реализован:** частично — Фаза 1 из 12.

**Что сделано (Ф1):** документная модель и API `/documents` поверх существующих хранилищ; черновик и «Применить»; история правок с автором, временем и перечнем изменений + откат; защита от перезаписи чужих правок (`rev` + условный UPDATE); права на запись решает сервер; блокировка правки на время расчёта; присутствие «кто редактирует»; входной файл заказчика неприкосновенен (в том числе на старом пути сохранения); проценты доп. расходов на уровне проекта.

**Что осталось:** фазы 2–12.

**Ближайший шаг:** Фаза 2 — ядро редактора на фронтенде, адаптер плоских строк, слой буфера обмена и перевод перечня на новый редактор.

**Требует решения пользователя перед Ф5:** список активных тендерных смет, которые нельзя трогать автоматической миграцией.

**Не проверено автоматикой:** миграция 042 прогонялась только на SQLite-схеме тестов; на PostgreSQL её нужно накатить на копии прод-базы перед деплоем (`alembic upgrade head`).
