# 🚀 Быстрый старт - Smeta AI

Полностью готовый к использованию сервис. Выберите способ запуска:

## ⚡ Способ 1: Локальный Python (быстро, для разработки)

### Требования
- Python 3.9+
- CLAUDE_API_KEY ключ

### Шаги

```bash
# 1. Подготовка
cp .env.example .env
# Отредактируйте .env и добавьте CLAUDE_API_KEY

# 2. Установка
pip install -r backend/requirements.txt

# 3. Инициализация
python init_pricelists.py

# 4. Запуск
python -m uvicorn backend.main:app --reload

# 5. Открыть в браузере
# http://localhost:8000
```

**Время запуска**: ~5 минут  
**Сложность**: Низкая

---

## 🐳 Способ 2: Docker Compose (рекомендуется для локального)

### Требования
- Docker Desktop установлен
- CLAUDE_API_KEY ключ

### Шаги

```bash
# 1. Подготовка
cp .env.example .env
# Отредактируйте .env и добавьте CLAUDE_API_KEY

# 2. Запуск
docker-compose up -d

# 3. Дождитесь инициализации БД (~30 сек)
docker-compose logs -f

# 4. Открыть в браузере
# http://localhost:8000

# Прекратить когда нужнo
docker-compose down
```

**Время запуска**: ~2 минуты  
**Сложность**: Низкая  
**Преимущества**: Идентично production окружению

---

## ☁️ Способ 3: Render.com (рекомендуется для production)

### Требования
- GitHub аккаунт с репозиторием smeta-ai
- Render.com аккаунт (бесплатный)
- CLAUDE_API_KEY ключ

### Шаги

1. **Загрузить на GitHub** (если еще не загружен)
```bash
git push origin main
```

2. **Перейти на https://render.com**

3. **Создать Web Service**
   - Выбрать репозиторий smeta-ai
   - Выбрать Docker окружение

4. **Добавить переменные окружения:**
   ```
   CLAUDE_API_KEY=sk-...
   USER_PASSWORD=ваш_пароль
   ADMIN_PASSWORD=ваш_пароль
   JWT_SECRET=ваш_секрет
   ```

5. **Deploy** - Готово! ✅

**Время запуска**: ~10 минут  
**Стоимость**: ~$14/месяц  
**Сложность**: Средняя  
**Преимущества**: Production-ready, масштабируемость, SSL, атвоматический деплой

Детали: см. `DEPLOYMENT.md`

---

## 🧪 Способ 4: Bash скрипт (полностью автоматизированный)

```bash
bash start.sh
```

Скрипт автоматически:
- Проверяет Python и pip
- Создает виртуальное окружение
- Устанавливает зависимости
- Инициализирует прайс-листы
- Показывает дальнейшие инструкции

---

## ✅ Проверка что всё работает

### 1. Из браузера
Откройте: **http://localhost:8000**

Вы должны увидеть экран входа Smeta AI

### 2. Тестовый логин
```
Пароль: admin123 (или ваш ADMIN_PASSWORD)
```

Вы должны войти на главную страницу

### 3. Проверка API
```bash
curl http://localhost:8000/api/health
# Ответ: {"status":"ok","service":"Smeta AI"}
```

### 4. Документация API
```
http://localhost:8000/docs
```

Интерактивная документация всех endpoints

---

## 🎯 Что дальше?

### Использование приложения

1. **Загрузить документы** - PDF, Excel, XML, GSN
2. **Выбрать тип входных данных** - ТЗ, Проект, Смета и т.д.
3. **Выбрать результаты** - Перечень, Смета, Сравнительный анализ
4. **Нажать "Сформировать"**
5. **Скачать результаты**

### Для администраторов

1. **Логин с ADMIN_PASSWORD**
2. **Открыть Админ-панель** (кнопка сверху)
3. **Смотреть статистику** - всё запросы, успешность
4. **Экспортировать в CSV**

---

## 📚 Полная документация

| Документ | Содержание |
|----------|-----------|
| **README.md** | Общее описание, требования, функциональность |
| **DEPLOYMENT.md** | Подробное развертывание на Render.com |
| **DOCKER_LOCAL.md** | Docker Compose инструкции, troubleshooting |
| **ARCHITECTURE.md** | Архитектура, технический стек, будущие улучшения |
| **FILES.md** | Полный список всех файлов и их назначения |

---

## 🆘 Частые проблемы

### Проблема: "CLAUDE_API_KEY не установлен"

**Решение:**
```bash
# Отредактируйте .env
nano .env

# Добавьте вашу API ключ:
CLAUDE_API_KEY=sk-XXXXXXXXXXXXXXXXXXXX

# Перезапустите приложение
```

### Проблема: "Port 8000 is already in use"

**Решение для Python:**
```bash
# Используйте другой порт
python -m uvicorn backend.main:app --port 8001 --reload

# Откройте в браузере
# http://localhost:8001
```

**Решение для Docker:**
```bash
# В docker-compose.yml измените:
ports:
  - "8001:8000"  # вместо 8000:8000
```

### Проблема: "Database connection refused"

**Решение:**
1. Убедитесь что Docker запущен
2. Подождите 30-60 секунд для инициализации БД
3. Проверьте логи: `docker-compose logs db`

### Проблема: "ModuleNotFoundError: No module named 'anthropic'"

**Решение:**
```bash
# Переустановите зависимости
pip install -r backend/requirements.txt --force-reinstall
```

---

## 🔐 Безопасность по умолчанию

```
USER_PASSWORD=user123
ADMIN_PASSWORD=admin123
JWT_SECRET=your-jwt-secret-here
```

**⚠️ IMPORTANTE**: Измените эти значения в production!

```bash
# Генерируйте сложные пароли
openssl rand -hex 16

# Генерируйте JWT секрет
python3 -c "import secrets; print(secrets.token_hex(32))"
```

---

## 📊 Примеры использования

### Пример 1: Простое ТЗ

1. Загрузите PDF с описанием работ
2. Выберите "ТЗ"
3. Выберите "Перечень работ и материалов" и "Смета"
4. Нажмите "Сформировать"

### Пример 2: Полный анализ

1. Загрузите ТЗ (PDF)
2. Загрузите Проект (Excel)
3. Выберите "ТЗ + Проект"
4. Выберите все три результата
5. Получите полный анализ с рекомендациями

### Пример 3: Проверка сметы

1. Загрузите готовую смету (Excel)
2. Загрузите проект/спецификацию (Excel)
3. Выберите "Смета ЭДЦ + Проект"
4. Выберите "Сравнительный анализ"
5. Получите подробный отчет о расхождениях

---

## 🎓 Обучение

### Видео-туториалы (рекомендуется добавить)
- [ ] Как загрузить файлы
- [ ] Как создать смету
- [ ] Как использовать админ-панель

### Пример файлов (рекомендуется создать)
- [ ] Образец ТЗ (PDF)
- [ ] Образец проекта (Excel)
- [ ] Образец готовой сметы

---

## 💬 Получение помощи

1. **Читайте документацию** - README.md, DEPLOYMENT.md
2. **Проверяйте логи** - `docker-compose logs -f`
3. **Смотрите примеры** - Этот файл содержит примеры
4. **Используйте API docs** - http://localhost:8000/docs

---

## 🎉 Поздравляем!

Вы успешно развернули **Smeta AI**!

Теперь вы можете:
- ✅ Загружать документы любого формата
- ✅ Автоматически создавать сметы
- ✅ Анализировать соответствие проектам
- ✅ Экспортировать результаты в Excel/PDF
- ✅ Просматривать историю всех задач

---

**Версия:** 1.0.0  
**Статус:** Ready for Use  
**Последнее обновление:** Февраль 2025

Приятного использования! 🚀
