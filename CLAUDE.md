# smeta-ai — навигация по проекту

Этот документ — карта проекта. Читай его в начале каждой сессии.

## Что это за проект

**smeta-ai** — внутренний AI-сервис CityManage для автоматизации подготовки смет и участия в строительных тендерах.
Сервис помогает отделу продаж быстро «перебивать» чужие сметы в корпоративный формат, проверять полноту и готовить сумму контракта до аукциона — вместо нескольких часов ручной работы.

Подробности — в `.business/INDEX.md`.

## Структура репозитория

```
smeta-ai/
├── CLAUDE.md             ← этот файл, навигация
├── .business/            ← бизнес-контекст (не в git)
│   ├── INDEX.md          ← оглавление, входная точка
│   ├── company/          ← about, team, values, legal
│   ├── products/         ← overview, pricing, product-1
│   ├── audience/         ← avatar, segments, objections, journey
│   ├── goals/            ← annual, quarterly, monthly, kpi
│   ├── economics/        ← unit-economics, revenue, costs, forecast
│   ├── marketing/        ← channels, funnel, competitors, content
│   └── assets/           ← brand-guidelines, testimonials
├── .planning/            ← GSD-планирование (roadmap, фазы, планы)
├── plans/                ← технические планы (один план = одна функция)
├── backend/              ← FastAPI-приложение (Python)
│   ├── app/              ← роуты, сервисы, модели, конфиг
│   ├── alembic/          ← миграции БД
│   └── tests/            ← pytest
├── frontend/             ← React-приложение (TypeScript + Vite)
│   └── src/              ← компоненты, страницы, стор, утилиты
├── docker-compose.yml    ← прод-топология для Timeweb (web+worker, managed PG)
├── docker-compose.dev.yml ← локальное окружение (db+volumes+reload+frontend)
└── render.yaml           ← деплой на Render.com
```

### Где что искать

| Что нужно | Куда смотреть |
|---|---|
| Зачем делаем, бизнес-логика | `.business/INDEX.md` |
| Пользователи, боли, желания | `.business/audience/` |
| Функции продукта, форматы | `.business/products/` |
| Цели и метрики | `.business/goals/` |
| Экономика | `.business/economics/` |
| Брендинг, стиль интерфейса | `.business/assets/brand-guidelines.md` |
| Технические планы | `plans/` |
| AI-промпты и сервисы | `backend/app/services/` |

### Единый редактор таблиц

Все пять типов документов (перечень, полнота, смета, версии оптимизации, разделы сводной) редактируются **одним компонентом**. Документ = `(карточка сметы, тип документа)`.

| Что нужно | Куда смотреть |
|---|---|
| HTTP-контракт документов | `backend/app/routers/documents.py` |
| Права, черновик, «Применить», история, откат, присутствие | `backend/app/services/document_service.py` |
| **Единственная точка записи сметы** (строки + файл + `task.cost`) | `backend/app/services/estimate_store.py` |
| Оболочка редактора | `frontend/src/components/editor/DocumentEditor.tsx` |
| Отличия типов документов (колонки, пересчёт, итоги, подсветка) | `frontend/src/components/editor/adapters/` |
| Расчёт итогов сметы — один на весь проект | `frontend/src/utils/estimateCalc.ts` |
| Правило выбора цены работы из прайса | `backend/app/utils/price_min.py` |
| Поиск аналогов через ИИ | `backend/app/services/analogs_service.py` |
| Листы исходного файла (вкладки документа) | `backend/app/utils/sheet_names.py` |
| Нормы расхода материалов каркасных систем | `backend/app/data/material_kits.py` |
| Разворачивание комплекта материалов к работе | `backend/app/services/material_kits.py` |
| Номер позиции исходной сметы в перечне | `backend/app/utils/source_numbers.py` |

**Правила, которые легко нарушить:**

1. **У строки один писатель.** Смета живёт только в рабочей версии (`EstimateVersion`); `task.progress_data['items']` заморожен как «что выдал ИИ». Строки раздела сводной — только в `summary_estimates.sections`.
2. **Входной файл заказчика не перезаписывается никогда.**
3. **Цены в таблице показываются уже с коэффициентом,** в документ уходят исходные. Исходная цена едет рядом со строкой (`__base_price_work` / `__base_price_material`).
4. **Экран и скачиваемый файл считают по одной формуле.** Меняете расчёт — меняйте в обоих местах и проверяйте тестом с точными числами.
5. **Нормы расхода правятся только в справочнике.** Комплект материалов к
   каркасной работе (перегородка, облицовка, потолок ГКЛ/ГВЛ) считается
   детерминированно на шаге «Полнота»: норма на 1 м² × объём работы, слои и
   стороны читаются из наименования. Каждая норма закреплена тестом с точным
   числом. Объём — чистый, без запаса на подрезку. Материал заказчика не
   задваивается и его объём не правится: расхождение помечается, решает человек.
6. **Правки перечня и полноты обязаны доезжать до сметы.** Смета строится из
   `task.progress_data['items']`, поэтому сохранение строк документа пишет и
   туда (`document_service._sync_task_items`). Забудете — правка останется в
   редакторе, а в смету уедет старое число, молча.
7. **Номер позиции исходной сметы через ИИ не гоняется.** «№ в исходной смете» —
   первая колонка перечня из Гранд-сметы. Заполняется детерминированно:
   позиции чанка сопоставляются со строками того же чанка по наименованию
   (`source_numbers.attach_source_numbers`). Выдуманный номер хуже пустой
   ячейки, поэтому неоднозначное совпадение не берётся вовсе. Колонка есть
   только у перечня: в промпт полноты поле не уходит.
8. **Лист исходного файла — поле строки (`sheet`), а не отдельная сущность.** Он же вкладка редактора и лист в скачиваемом файле. Вкладка — только фильтр показа: в состоянии редактора всегда лежат строки **всех** вкладок, иначе «Применить» сотрёт закрытые разделы. Разбираете xlsx — разбирайте все листы; собираете xlsx — пишите лист на группу через `safe_sheet_title`.

## Ключевые принципы проекта

1. **Итеративно, не всё сразу.** Один тип задачи → промпт → тест → следующий. Не строим следующее, пока текущее не работает корректно.
2. **Внутренний инструмент.** Пользователи — менеджеры и руководитель отдела продаж CityManage. Онбординг не нужен. UX — рабочий, не маркетинговый. Нет публичного лендинга — главная страница сервиса сразу показывает форму входа по паролю.
3. **Корректность важнее скорости.** Ошибка в смете = финансовые потери на тендере. Каждый тип задачи тестируется перед вводом в эксплуатацию.

## Стек

**Backend:** Python 3.12, FastAPI, SQLAlchemy 2.0 (async), PostgreSQL 16, Alembic, Anthropic Claude API, openpyxl, WeasyPrint.

**Frontend:** React 18, TypeScript, Vite, React Router 6, Zustand, Axios.

**Инфраструктура:** Docker Compose (локально), Render.com (прод), Nginx (frontend).

## Папка `.business/`

Скрытая папка с бизнес-контекстом. Здесь живёт информация **зачем** мы делаем проект. Не попадает в git.

## Папка `plans/`

Технические планы реализации. Здесь живёт информация **как** мы делаем проект. Один план = одна функция.

## Как работать с проектом

- Перед любой задачей — прочитай этот файл, затем нужные документы из `.business/` и `plans/`.
- Если меняется бизнес-логика — обнови `.business/INDEX.md` и нужные файлы.
- Если меняется технический план — обнови соответствующий план в `plans/`.
- Если появляется новая важная папка или документ — обнови этот `CLAUDE.md`.

## ВАЖНО: изменения моделей базы данных

При любом изменении `backend/app/models/*.py` — сначала создать файл миграции в `backend/alembic/versions/0NN_описание.py` (следующий номер по порядку), потом коммит. Всегда использовать `IF NOT EXISTS`. Шаблон — любой существующий файл в `backend/alembic/versions/`, например `013_add_composite_index_tasks_project_estimation.py`.

## ВАЖНО: план для каждой новой функции

Любая функция, которую мы создаём в любом чате, всегда оформляется планом в папке `plans/`.

Правила:

1. Один план = одна функция. Если план уже есть — работаем с ним.
2. Имя файла: `YYYY-MM-DD-название-функции.md`.
3. План делится на фазы. У каждой фазы статус `[ ]` или `[x]`.
4. В конце плана — итоговый блок: реализован целиком или нет, что осталось.
5. Любой агент обязан актуализировать план после каждой сессии.

## ВАЖНО: завершение каждого чата

В конце каждой сессии записывай рефлексию в файл `.business/история/YYYY-MM-DD-краткое-название.md` (создавай папку при необходимости).

Формат:

1. Какая задача была поставлена.
2. Как я её решал.
3. Решил ли — да / нет / частично.
4. Эффективно ли решение, что можно было лучше.
5. Как было и как стало.

## ВАЖНО: коммит после каждой задачи

После каждой завершённой задачи — git commit с осмысленным сообщением.

## ВАЖНО: Bulletproof для фич

Каждый раз, когда пользователь ставит задачу на новую функцию — всегда использовать skill `bulletproof`.

**Исключение:** статические страницы (документация, внутренние презентации) — без bulletproof, работаем напрямую.

## ВАЖНО: сверяйся с .business

- При работе с текстами — читай `.business/audience/avatar.md`.
- При работе с UI — читай `.business/assets/brand-guidelines.md`.

## ВАЖНО: безопасность

Запрещено добавлять в settings.json: `rm -rf`, `curl` с экспортом данных, доступ к `.env`.

## Язык

Всегда отвечай на русском.
